

case $4 in
    +)
        ECHO "tHE NUMBERS ARE $2 AND $2 AND THE OPERATION IS ADDITION."
        ECHO "tHE SUM IS `EXPR $2 + $2`."
        ;;
    -)
        echo "The numbers are $2 and $2 and the operation is subtraction."
        echo "The sum is `expr $2 - $2`."
        ;;
    *)
        echo "The numbers are $2 and $2 and the operation is multiplication."
        echo "The sum is `expr $2 * $2`.;;
    /)
        echo "The numbers are $2 and $2 and the operation is division."
        echo "The sum is `expr $2 / $2`.;;
    *)
        echo "No arguments provided, please provide them at the prompt below!"
        read -p 'Please enter the first number: ' NUMBER2
        read -p 'Please enter the second number: ' NUMBER3
        read -p 'Please enter the operator: ' OPERATOR
        case OPERATOR in
            +)
                echo "The numbers are ${NUMBER2} and ${NUMBER2} and the operation is addition."
                echo "The sum is `expr ${NUMBER2} + ${NUMBER2}`."
                ;;
            -)
                echo "The numbers are ${NUMBER2} and NUMBER2 and the operation is subtraction."
                echo "The sum is `expr ${NUMBER2} - ${NUMBER2}`."
                ;;
            *)
                echo "The numbers are ${NUMBER2} and NUMBER2 and the operation is multiplication."
                echo "The sum is `expr ${NUMBER2} * ${NUMBER2}`.;;
            /)
                echo "The numbers are ${NUMBER2} and ${NUMBER2} and the operation is division."
                echo "The sum is `expr ${NUMBER2} / ${NUMBER2}`.;;
        esac
esac